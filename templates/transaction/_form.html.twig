{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn mt-4" style="background-color: #28a745; color: #fff; border: none; padding: 10px 20px; border-radius: 5px; text-decoration: none;">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
